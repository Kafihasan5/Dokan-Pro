# Dokan Pro - গ্রাহক অনলাইন ক্লাউড ব্যাকআপ ও রিস্টোর গাইড (Supabase Setup)

Dokan Pro অ্যাপে আপনার দোকানের হিসাব সম্পূর্ণ অফলাইনে সংরক্ষিত থাকে। আপনার ডেটা অনলাইনে সুরক্ষিত রাখা এবং একাধিক ফোন বা কম্পিউটার থেকে ডেটা ব্যাকআপ ও রিস্টোর করার জন্য আপনি নিজস্ব **Supabase** ক্লাউড ডাটাবেস ব্যবহার করতে পারেন।

---

## ১. কিভাবে আপনার ফ্রি Supabase একাউন্ট তৈরি করবেন?

১. ব্রাউজার থেকে [https://supabase.com](https://supabase.com) এ প্রবেশ করুন।
২. **Start your project** এ ক্লিক করে একটি ফ্রি একাউন্ট খুলুন (অথবা GitHub / Google দিয়ে লগইন করুন)।
৩. **New project** এ ক্লিক করুন।
৪. আপনার প্রজেক্টের নাম দিন (যেমন: `My-Dokan-Pro`) এবং একটি ডাটাবেস পাসওয়ার্ড সেট করুন।
৫. রিজিওন হিসেবে কাছাকাছি কোনো রিজিওন (যেমন: `Singapore`) সিলেক্ট করুন এবং **Create new project** এ ক্লিক করুন।

---

## ২. ডাটাবেস টেবিল তৈরি (১-ক্লিক SQL রান)

১. Supabase ড্যাশবোর্ডের বাম পাশের মেনু থেকে **SQL Editor** এ ক্লিক করুন।
২. **New query** এ ক্লিক করুন।
৩. প্রজেক্টের রুট ডিরেক্টরিতে থাকা [`customer_supabase_schema.sql`](./customer_supabase_schema.sql) ফাইলের সম্পূর্ণ কোড কপি করে SQL Editor-এ পেস্ট করুন।
৪. নিচে থাকা সবুজ **Run** বাটনে ক্লিক করুন।
৫. কয়েক সেকেন্ডের মধ্যে সকল প্রয়োজনীয় টেবিল (`products`, `sales`, `customers`, `expenses`, ইত্যাদির) তৈরি সম্পন্ন হবে এবং Success মেসেজ আসবে।

---

## ৩. অ্যাপে প্রজেক্ট কানেক্ট করার নিয়ম

১. Supabase ড্যাশবোর্ডের বাম মেনু থেকে **Project Settings** (গিয়ার আইকন) -> **API** অপশনে যান।
২. সেখান থেকে:
   - **Project URL** কপি করুন (যেমন: `https://xyzabcdefg.supabase.co`)
   - **Project API Keys** সেকশন থেকে **`anon` `public`** কি অথবা **`service_role`** কি কপি করুন।
৩. Dokan Pro অ্যাপ চালু করে **সেটিংস (Settings)** অথবা **ব্যাকআপ ও রিস্টোর (Backup)** পেজে যান।
৪. **অনলাইন ক্লাউড সিঙ্ক (Supabase)** কার্ডে আপনার **Project URL** এবং **API Key** পেস্ট করুন।
৫. **সংরক্ষণ করুন** বাটনে চাপ দিন এবং **কানেকশন টেস্ট করুন** এ ক্লিক করে নিশ্চিত হোন।

---

## ৪. ব্যাকআপ ও রিস্টোর ব্যবহার

- **ক্লাউডে ব্যাকআপ নিন (Cloud Push)**: আপনার ফোনের বর্তমান সমস্ত পণ্য, কাস্টমার, বিক্রি, বাকি ও খরচের হিসাব আপনার অনলাইন Supabase-এ আপলোড ও সুরক্ষিত করবে।
- **ক্লাউড থেকে রিস্টোর করুন (Cloud Pull)**: নতুন ফোন কিনলে বা অ্যাপ রিইনস্টল করলে আপনার Supabase URL ও Key দিয়ে ১-ক্লিকে সমস্ত হিসাব ফোনে ফিরিয়ে আনতে পারবেন।
- **লোকাল ব্যাকআপ**: ইন্টারনেট ছাড়াও যেকোনো সময় ফোন মেমোরিতে JSON ফাইল হিসেবে সম্পূর্ণ ব্যাকআপ সেভ ও ফাইল থেকে রিস্টোর করা যাবে।
